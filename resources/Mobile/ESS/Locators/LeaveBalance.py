requestsFilterButtonLabel = "requests.label.filter_requests"
requestsBalanceLabelBalanceAsOf = "requests.balance.label.balance_as_of"
requestsTabBalanceTitle = "requests.tabs.title.balance"

# Native
Leave_Balance_Date_Label_Native = (
    '//*[@resource-id and substring-after(@resource-id, ":id/")="accrualTxt2"]'
)
balance_as_of_now_down_arrow = (
    '//android.widget.ImageView[contains(@content-desc, "{0}")]'
)
